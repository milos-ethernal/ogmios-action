name: Test

on: 
  workflow_dispatch:
  push:
    branches:
      - master

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

    - name: Set up Go
      uses: actions/setup-go@v2
      with:
        go-version: '1.20'
    
    # - name: Install ogmios
    #   uses: milos-ethernal/ogmios-action@c19d5ec711f2f5c6e7712dfce00e3f065609e212

    # - name: Test
    #   run: ogmios --version
